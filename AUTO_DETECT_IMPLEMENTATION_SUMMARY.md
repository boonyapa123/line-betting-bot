# Auto-Detect GROUP_ID Implementation Summary

## 📋 สิ่งที่ทำ

### 1. สร้าง Service ใหม่: `GroupAutoDetectService`
**ไฟล์:** `src/services/groupAutoDetectService.ts`

**ฟังก์ชันหลัก:**
- `handleJoinEvent()` - จัดการเมื่อ OA ถูกเพิ่มเข้ากลุ่ม
- `handleLeaveEvent()` - จัดการเมื่อ OA ถูกลบออกจากกลุ่ม
- `getPrimaryGroupId()` - ดึง Group ID แรกที่ถูกตรวจจับ
- `getAllGroups()` - ดึงกลุ่มทั้งหมด

**ฟังก์ชันภายใน:**
- `saveGroupLocally()` - บันทึกกลุ่มลงใน `data/groups.json`
- `removeGroupLocally()` - ลบกลุ่มออกจาก `data/groups.json`
- `updateEnvFile()` - อัปเดต `.env` ไฟล์ด้วย `LINE_GROUP_ID`
- `sendWelcomeMessage()` - ส่งข้อความต้อนรับไปยังกลุ่ม

### 2. อัปเดต `app.ts`
**การเปลี่ยนแปลง:**
- เพิ่ม import `GroupAutoDetectService`
- เพิ่ม handler สำหรับ `join` event
- เพิ่ม handler สำหรับ `leave` event
- เพิ่ม API endpoint `/api/groups` เพื่อดูกลุ่มที่ถูกตรวจจับ

### 3. อัปเดต `officialAccountService.ts`
**การเปลี่ยนแปลง:**
- เพิ่ม import `GroupAutoDetectService`
- เปลี่ยนวิธีการหา `groupId`:
  - ก่อนอื่นลองหาจาก `process.env.LINE_GROUP_ID`
  - ถ้าไม่มี ให้ใช้ `GroupAutoDetectService.getPrimaryGroupId()`
- ปรับปรุง error message ให้ชัดเจนขึ้น

### 4. อัปเดต `.env`
**การเปลี่ยนแปลง:**
- เพิ่ม `LINE_GROUP_ID=` (ว่างไว้ให้ระบบ auto-fill)

## 🔄 Flow การทำงาน

```
1. OA ถูกเพิ่มเข้ากลุ่ม
   ↓
2. LINE ส่ง join event ไปยัง webhook
   ↓
3. app.ts รับ event และเรียก GroupAutoDetectService.handleJoinEvent()
   ↓
4. Service ทำการ:
   - ดึง Group ID จาก event
   - ดึงชื่อกลุ่มจาก LINE API
   - บันทึกลงใน data/groups.json
   - อัปเดต .env ไฟล์
   - ส่งข้อความต้อนรับ
   ↓
5. ผู้ใช้ได้รับข้อความต้อนรับ
   ↓
6. ผู้ใช้รีสตาร์ท server
   ↓
7. ตอนนี้ LINE_GROUP_ID ถูกโหลดจาก .env
```

## 📊 ข้อมูลที่บันทึก

### `data/groups.json`
```json
{
  "C1234567890abcdef1234567890abcdef": {
    "id": "C1234567890abcdef1234567890abcdef",
    "name": "ห้องแทงบั้งไฟ",
    "joinedAt": "2024-01-10T12:34:56.000Z",
    "lastActive": "2024-01-10T12:34:56.000Z"
  }
}
```

### `.env` (auto-updated)
```env
LINE_GROUP_ID=C1234567890abcdef1234567890abcdef
```

## 🎯 ประโยชน์

✅ **ไม่ต้องตั้งค่าด้วยตนเอง** - ระบบจะตรวจจับอัตโนมัติ  
✅ **ง่ายต่อการใช้งาน** - เพียงเพิ่ม OA เข้ากลุ่มก็เสร็จ  
✅ **รองรับหลายกลุ่ม** - สามารถเพิ่ม OA เข้าหลายกลุ่มได้  
✅ **บันทึกข้อมูล** - ทั้งหมดบันทึกใน `data/groups.json`  
✅ **ข้อความต้อนรับ** - ส่งข้อความต้อนรับอัตโนมัติ  

## 🧪 ทดสอบ

### 1. ทดสอบ Join Event
```bash
# เพิ่ม OA เข้ากลุ่ม LINE
# ดู console logs เพื่อยืนยัน join event
```

### 2. ตรวจสอบ Groups
```bash
curl https://your-domain.com/api/groups
```

### 3. ตรวจสอบ .env
```bash
cat .env | grep LINE_GROUP_ID
```

### 4. ทดสอบคำสั่ง
```bash
# ส่งข้อความจาก OA (1-on-1)
# ตรวจสอบว่าข้อความปรากฏในกลุ่ม
```

## 📝 Documentation

- **GROUP_AUTO_DETECT_GUIDE.md** - คู่มือการใช้งาน
- **src/services/groupAutoDetectService.ts** - Code comments

## ⚠️ หมายเหตุ

1. **ต้องรีสตาร์ท Server** - หลังจากอัปเดต `.env` ต้องรีสตาร์ท server เพื่อให้ environment variables โหลดใหม่

2. **Multiple Groups** - ถ้าเพิ่ม OA เข้าหลายกลุ่ม:
   - ทั้งหมดจะบันทึกใน `data/groups.json`
   - `LINE_GROUP_ID` ใน `.env` จะเป็นกลุ่มแรก
   - สามารถใช้ `/api/groups` เพื่อดูทั้งหมด

3. **Leave Event** - เมื่อ OA ถูกลบออกจากกลุ่ม:
   - กลุ่มจะถูกลบออกจาก `data/groups.json`
   - `.env` ไม่เปลี่ยนแปลง

## 🚀 ขั้นตอนถัดไป

1. Deploy code ใหม่
2. เพิ่ม OA เข้ากลุ่ม LINE
3. ตรวจสอบ console logs
4. รีสตาร์ท server
5. ทดสอบคำสั่ง
