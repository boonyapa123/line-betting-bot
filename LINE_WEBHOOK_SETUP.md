# ตั้ง Webhook ใน LINE - ทีละขั้นตอน

## ขั้นตอนที่ 1: ไปที่ LINE Developers Console

1. เปิด https://developers.line.biz/console/
2. Login ด้วย LINE account ของคุณ
3. เลือก **Channel** ของคุณ (line-betting-bot)

---

## ขั้นตอนที่ 2: ไปที่ Messaging API Settings

1. ในหน้า Channel ให้ไปที่ **"Messaging API"** tab
2. Scroll ลงไปหา **"Webhook URL"** section

---

## ขั้นตอนที่ 3: ใส่ Webhook URL

1. ในช่อง **"Webhook URL"** ให้ใส่:
   ```
   https://line-betting-bot.onrender.com/webhook
   ```

2. Click **"Verify"** เพื่อทดสอบ
   - ถ้าสำเร็จ จะเห็น ✅ "Verified"
   - ถ้าล้มเหลว จะเห็น ❌ error message

---

## ขั้นตอนที่ 4: Enable "Use webhook"

1. หา toggle **"Use webhook"**
2. เปลี่ยนเป็น **ON** (สีเขียว)

---

## ✅ เสร็จแล้ว!

Webhook ของคุณตั้งค่าเสร็จแล้ว

---

## 🧪 ทดสอบ

### วิธีที่ 1: ทดสอบใน LINE
1. เพิ่ม Bot เป็นเพื่อน
2. พิมพ์ข้อความใดๆ
3. ดู Logs ใน Render Dashboard เพื่อดูว่า webhook ได้รับข้อมูลไหม

### วิธีที่ 2: ดู Logs ใน Render
1. ไปที่ Render Dashboard: https://dashboard.render.com/
2. Click ที่ service `line-betting-bot`
3. ไปที่ **"Logs"** tab
4. ดู logs เพื่อตรวจสอบว่า webhook ทำงานไหม

---

## 🔧 Troubleshooting

### "Verify failed" - 404 Not Found
**ปัญหา:** Webhook URL ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ URL ถูกต้องไหม: `https://line-betting-bot.onrender.com/webhook`
2. ตรวจสอบ Render deployment สำเร็จไหม
3. ลองรอ 1-2 นาที แล้ว verify อีกครั้ง

### "Verify failed" - 500 Internal Server Error
**ปัญหา:** Server มี error

**วิธีแก้:**
1. ดู Logs ใน Render Dashboard
2. ตรวจสอบ environment variables ครบไหม
3. ตรวจสอบ Google Sheets credentials ถูกต้องไหม

### Bot ไม่ตอบข้อความ
**ปัญหา:** Webhook ไม่ทำงาน

**วิธีแก้:**
1. ตรวจสอบ "Use webhook" เปิดไหม
2. ดู Logs ใน Render Dashboard
3. ตรวจสอบ Webhook URL ถูกต้องไหม

---

## 📝 Notes

- Webhook URL ต้องเป็น HTTPS (ไม่ใช่ HTTP)
- Render ให้ HTTPS โดยอัตโนมัติ
- ต้องรอ 1-2 นาที ให้ LINE ส่ง webhook ครั้งแรก

