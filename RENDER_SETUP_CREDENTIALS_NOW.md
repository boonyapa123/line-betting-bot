# 🚀 ตั้งค่า Google Credentials ใน Render ตอนนี้

## ปัญหาปัจจุบัน
- ⚠️ `.env file not found` บน Render
- ❌ `/api/groups` endpoint ล้มเหลว
- ❌ LIFF forms ติดอยู่ที่ "กำลังโหลด"

## วิธีแก้ (ทำตามขั้นตอน)

### ขั้นตอนที่ 1: เตรียม Credentials JSON

1. เปิดไฟล์ `linebot-482513-5e72ad3d3232.json` ในโปรเจกต์
2. Copy **ทั้งหมด** (Ctrl+A, Ctrl+C)

### ขั้นตอนที่ 2: ไปที่ Render Dashboard

1. เข้า https://dashboard.render.com
2. เลือก Project: **line-betting-bot**
3. ไปที่ **Settings** (แท็บด้านบน)

### ขั้นตอนที่ 3: เพิ่ม Secret File

1. ไปที่ **Environment** (ในเมนู Settings)
2. ค้นหา **Secret Files** section
3. คลิก **Add Secret File**
4. กรอกข้อมูล:
   - **Filename:** `google-credentials.json`
   - **Content:** (Paste ที่ copy มาจากขั้นตอนที่ 1)
5. คลิก **Save**

### ขั้นตอนที่ 4: เพิ่ม Environment Variable

1. ยังอยู่ใน **Environment** section
2. ค้นหา **Environment Variables** section
3. คลิก **Add Environment Variable**
4. กรอกข้อมูล:
   - **Key:** `GOOGLE_CREDENTIALS_PATH`
   - **Value:** `/etc/secrets/google-credentials.json`
5. คลิก **Save**

### ขั้นตอนที่ 5: Redeploy

1. ไปที่ **Deployments** (แท็บด้านบน)
2. หา latest deployment
3. คลิก **Redeploy** (ปุ่มสามจุด → Redeploy)
4. รอให้ deploy เสร็จ (ประมาณ 2-3 นาที)

### ขั้นตอนที่ 6: ตรวจสอบ

1. เปิด https://line-betting-bot.onrender.com/api/groups
2. ควรเห็น JSON response:
   ```json
   {
     "success": true,
     "groups": [
       {
         "id": "Ce73f7032aa63204dcfc2d5685719565b",
         "name": "bot line บั้งไฟ"
       }
     ],
     "count": 1
   }
   ```

3. ถ้าเห็นข้อมูลกลุ่ม = ✅ สำเร็จ!

## ถ้ายังไม่ได้

ให้ตรวจสอบ logs:
1. ไปที่ **Logs** (แท็บด้านบน)
2. ค้นหา "Google credentials" หรือ "Error"
3. ส่งข้อความ error ให้ฉันดู

## หมายเหตุ

- ⚠️ ห้ามใส่ `GOOGLE_CREDENTIALS_JSON` environment variable (มันจะ truncate)
- ✅ ใช้ Secret File แทน (ไม่มีข้อจำกัดความยาว)
- 🔄 หลังจาก redeploy ให้รอ 2-3 นาทีให้เซิร์ฟเวอร์เริ่มต้นใหม่

